#put this file in /etc/systemd/system

[Unit]
Description=CCI Omero front-end running uwsgi and flask
After=network.target

[Service]
Type=simple
User=${WWW_USER}
Group=www-data
WorkingDirectory=${ROOT_DIR}
ExecStart=${ROOT_DIR}/start_uwsgi.sh
Restart=always

[Install]
WantedBy=multi-user.target
